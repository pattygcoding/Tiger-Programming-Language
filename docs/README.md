# Tiger Tutorial and Reference

Tiger combines dynamic values and first-class functions with explicit blocks, semicolons, and single-inheritance classes. This guide teaches the implemented language, not the full Python language or a planned feature set.

## Start Here

Begin with [Getting Started](getting-started.md), then follow the lessons below in order. Each lesson introduces a concept, gives a complete runnable example, and explains the rules that matter when writing larger programs.

| Lesson | Learn |
| --- | --- |
| [1. Getting Started](getting-started.md) | Run your first program and read its output |
| [2. Syntax](syntax.md) | Statements, blocks, comments, and names |
| [3. Variables and Scope](variables-and-scope.md) | Assignment, constants, lexical scope, and sharing |
| [4. Types and Operators](types-and-operators.md) | Numbers, booleans, equality, precedence, and truthiness |
| [5. Strings](strings.md) | Text, escapes, indexing, and conversion |
| [6. Lists and Dictionaries](collections.md) | Ordered data, keys, mutation, and iteration snapshots |
| [7. Control Flow](control-flow.md) | Conditions, loops, and early returns |
| [8. Functions and Closures](functions-and-closures.md) | Recursion, callbacks, and captured state |
| [9. Classes and Inheritance](classes-and-inheritance.md) | Instances, constructors, overrides, and lexical `super` |

## Reference

- [Importing Tiger Files](modules.md): relative imports, namespaces, caching, and standalone dependencies.
- [Built-ins](builtins.md): complete signatures and behavior for `print`, `str`, `len`, and `range`.
- [Editor Highlighting](syntax-highlighting.md): keywords, operators, literals, semantic categories, and editor checks.
- [Runtime and Errors](runtime-and-errors.md): diagnostics, evaluation limits, and unsupported features.
- [Running and Building](running-and-building.md): CLI, standalone executables, browser Wasm, WASI, and embedding.

## Work Through an Example

Each `tg` code block is a complete program with its own fresh environment. Run it in the browser editor or place it in a file ending in `.tg`. The output immediately below it is the exact standard output, without command prompts. Examples labeled **Expected error** deliberately fail; their error block shows a diagnostic substring rather than a filename-dependent message.

After running a lesson's example, try the suggested variation. These exercises reuse features already introduced; they do not require unimplemented libraries.

## Verified Documentation

The documentation tests execute every `tg` block in this directory and the root README, compare displayed outputs, and check expected-error examples. They also check local Markdown link targets exist.

```sh
go test ./docs -count=1 -v
```

Run that command from the repository root. `go test ./...` includes these checks too. Output examples are reference results, not regenerated by the test runner.

For larger programs, explore the [examples](../examples/demo.tg) and the [seventeen regression workloads](../benchmarks/README.md).

[Begin: Getting Started](getting-started.md) | [Project README](../README.md)