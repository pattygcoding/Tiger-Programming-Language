const MAX_RUNS = 5;

def factorial(n) {
    if n <= 1 {
        return 1;
    }
    return n * factorial(n - 1);
}

print("Running Tiger demo:");

i = 1;
while i <= MAX_RUNS {
    val = factorial(i);
    print("factorial(" + str(i) + ") = " + str(val));
    i = i + 1;
}