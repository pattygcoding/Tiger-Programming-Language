const MAX_RUNS = 5;

function factorial(n) {
    if n <= 1 {
        return 1;
    }
    return n * factorial(n - 1);
}

print("Running Tiger demo:");

var i = 1;
while i <= MAX_RUNS {
    const val = factorial(i);
    print("factorial(" + str(i) + ") = " + str(val));
    i = i + 1;
}